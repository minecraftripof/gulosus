randint = 0;

x_speed = 0;

y_speed = 0;

movement_speed = 1;

up_free = true;

down_free = false;

left_free = false;

right_free = false;

true_x = (bbox_left + bbox_right) / 2;
true_y  = (bbox_top + bbox_bottom) / 2;

switch_movement = false;

move_chance = 120;

next_sprite = sNpcfForward;